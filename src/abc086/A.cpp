#include "bits/stdc++.h"
using namespace std;
typedef long long ll;

int main(int argc, char* argv[]){
    int a, b;
    cin >> a >> b;
    if(a%2==0 || b%2==0) cout << "Even";
    else cout << "Odd";
}